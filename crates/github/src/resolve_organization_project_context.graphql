query ResolveOrganizationProjectIssueContext(
  $owner: String!
  $repo: String!
  $number: Int!
  $projectOwner: String!
  $projectNumber: Int!
) {
  repository(owner: $owner, name: $repo) {
    issue(number: $number) {
      id
    }
  }
  organization(login: $projectOwner) {
    projectV2(number: $projectNumber) {
      id
    }
  }
}
